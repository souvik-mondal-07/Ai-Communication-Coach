/**
 * Placeholder hook for the future voice conversation feature
 * (microphone capture, speech-to-text, text-to-speech playback).
 */
export function useVoice() {
  return {
    isRecording: false,
  };
}
