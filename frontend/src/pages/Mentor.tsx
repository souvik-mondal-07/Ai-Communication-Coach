import { Bot } from "lucide-react";
import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function Mentor() {
  return (
    <PlaceholderPage
      icon={Bot}
      title="AI Mentor"
      description="Chat with your personal mentor for guidance, explanations, and study plans. The conversational AI engine is connected in a later step."
    />
  );
}
