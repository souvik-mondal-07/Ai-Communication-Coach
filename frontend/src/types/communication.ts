/**
 * Placeholder communication types. Populated fully when the communication practice feature is implemented.
 */
export interface CommunicationSession {
  id: string;
  topic: string;
  createdAt: string;
}
