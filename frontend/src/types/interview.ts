/**
 * Placeholder interview types. Populated fully when the interview practice feature is implemented.
 */
export interface InterviewSession {
  id: string;
  type: "hr" | "technical" | "behavioral";
  createdAt: string;
}
